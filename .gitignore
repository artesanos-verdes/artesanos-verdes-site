# Documents internes — NE JAMAIS publier
# Le site est servi par GitHub Pages : tout fichier commité devient public.
AUDIT_MITECO_CONSUMO.md

# admin.html est de nouveau publie depuis le 13 aout 2026 : il ne contient
# plus aucun mot de passe et passe par Firebase Authentication (Google).
# L'acces aux donnees est verrouille par la regle Firestore, qui n'autorise
# la lecture de 'leads' qu'a une adresse nominative verifiee.

# Configuration locale des outils
.claude/

# Fichiers de travail accidentels
pwd
test.txt

# Systeme
.DS_Store
Thumbs.db
